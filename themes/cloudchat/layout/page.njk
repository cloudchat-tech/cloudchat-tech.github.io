<article class="prose relative max-w-none lg:prose-xl -mt-1">
  <header>
    <h1 class="mb-1 pr-16">{{ page.title }}</h1>
  </header>
  <div class="-mt-6">
    {{ page.content }}
  </div>
  <div class="mt-4 text-right text-xs text-gray-500">
    <a href="{{ url_for(page.path) }}" class="mb-1 text-gray-500">Permalink</a>
  </div>
</article>