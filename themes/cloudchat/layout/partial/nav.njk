<nav class="bg-gray-800 mb-8">
    <div class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4">
        <div class="w-auto mx-auto">
            <ul class="flex flex-row rounded-lg md:space-x-8 bg-gray-800 md:dark:bg-transparent">
                {{ partial('partial/nav-link', { title: 'Home', href: url_for('/')}) }}
                {{ partial('partial/nav-link', { title: 'Episodes', href: url_for('/episodes')}) }}
                {{ partial('partial/nav-link', { title: 'About', href: url_for('/about')}) }}
                {{ partial('partial/nav-link', { title: 'Contact', href: url_for('/contact')}) }}
            </ul>
        </div>
    </div>
</nav>